name: images
flags:
  - set
  - set-file
  - set-string
  - values
  - skip-crds
  - skip-tests
commands:
  - name: get
    flags:
      - from-release
      - image-regex
      - json
      - kind
      - log-level
      - registry
      - table
      - unique
      - yaml
  - name: version