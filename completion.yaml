name: images
flags:
  - set
  - set-file
  - set-string
  - -s
  - show-only
  - skip-crds
  - skip-tests
  - validate
  - f
  - values
  - version
commands:
  - name: get
    flags:
      - from-release
      - image-regex
      - k
      - kind
      - l
      - log-level
      - no-color
      - r
      - registry
      - o
      - output
      - u
      - skip
      - unique
  - name: version